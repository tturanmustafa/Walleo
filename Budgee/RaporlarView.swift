import SwiftUI

struct RaporlarView: View {
    var body: some View {
        NavigationStack {
            Text("Raporlar Sayfası Yakında!")
                .navigationTitle("Raporlar")
        }
    }
}
