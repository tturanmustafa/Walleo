import SwiftUI
struct RaporlarView: View {
    var body: some View {
        NavigationStack {
            Text("reports.coming_soon") // DEĞİŞTİ
                .navigationTitle("tab.reports") // DEĞİŞTİ
        }
    }
}
