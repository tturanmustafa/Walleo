import SwiftUI
struct TakvimView: View {
    var body: some View {
        NavigationStack {
            Text("calendar.coming_soon") // DEĞİŞTİ
                .navigationTitle("tab.calendar") // DEĞİŞTİ
        }
    }
}
